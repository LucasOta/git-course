/**
 * Sum function. Recibe un array de numeros y los suma
 * @param {number[]} a
 * @returns {number}
 */
const sum = (a) => {
    const result = 0;
    a.map((n)=> result += n);
    return result;
};
